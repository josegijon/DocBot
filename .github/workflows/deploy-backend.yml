name: Deploy backend to HF Spaces

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Push backend to HF Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git clone https://josegijon:$HF_TOKEN@huggingface.co/spaces/josegijon/DocBot hf-space
          cp -r backend/. hf-space/
          cd hf-space
          git config user.email "ci@docbot"
          git config user.name "DocBot CI"
          git add .
          git diff --cached --quiet || git commit -m "deploy: sync from GitHub"
          git push
